cmake_minimum_required(VERSION 2.8)
project( currentTrafficVision )
find_package( OpenCV REQUIRED )
add_executable( currentTrafficVision currentTrafficVision.cpp )
target_link_libraries( currentTrafficVision ${OpenCV_LIBS} )
